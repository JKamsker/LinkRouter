<Page
    x:Class="LinkRouter.Settings.Views.RulesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:viewModels="using:LinkRouter.Settings.ViewModels"
    x:DataType="viewModels:RulesViewModel"
    mc:Ignorable="d">
    <Page.DataContext>
        <viewModels:RulesViewModel />
    </Page.DataContext>

    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <muxc:CommandBar Grid.ColumnSpan="2">
            <AppBarButton Icon="Add" Label="Add" Command="{x:Bind AddRuleCommand}" />
            <AppBarButton Icon="Delete" Label="Delete" Command="{x:Bind DeleteRuleCommand}" CommandParameter="{x:Bind SelectedRule}" />
            <AppBarButton Icon="Copy" Label="Duplicate" Command="{x:Bind DuplicateRuleCommand}" CommandParameter="{x:Bind SelectedRule}" />
            <AppBarButton Icon="Up" Label="Move up" Command="{x:Bind MoveUpCommand}" CommandParameter="{x:Bind SelectedRule}" />
            <AppBarButton Icon="Down" Label="Move down" Command="{x:Bind MoveDownCommand}" CommandParameter="{x:Bind SelectedRule}" />
        </muxc:CommandBar>

        <ListView Grid.Row="1"
                  ItemsSource="{x:Bind Rules}"
                  SelectedItem="{x:Bind SelectedRule, Mode=TwoWay}"
                  SelectionMode="Single">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="viewModels:RuleEditorViewModel">
                    <StackPanel Spacing="4">
                        <TextBlock Text="{x:Bind DisplayName}" />
                        <TextBlock Text="{x:Bind Browser}" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <ScrollViewer Grid.Column="1" Grid.Row="1" Margin="16,0,0,0">
            <StackPanel Spacing="12">
                <muxc:SettingsCard Header="Rule details">
                    <StackPanel Spacing="8" DataContext="{x:Bind SelectedRule, Mode=OneWay}">
                        <CheckBox Content="Enabled" IsChecked="{Binding Enabled, Mode=TwoWay}" />
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <ComboBox Width="160" ItemsSource="{x:Bind MatchTypes}" SelectedItem="{Binding Match, Mode=TwoWay}" />
                            <TextBox Width="320" PlaceholderText="Pattern" Text="{Binding Pattern, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                        <TextBox PlaceholderText="Browser path" Text="{Binding Browser, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBox PlaceholderText="Args template" Text="{Binding ArgsTemplate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBox PlaceholderText="Profile hint" Text="{Binding Profile, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBox PlaceholderText="User data dir" Text="{Binding UserDataDir, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBox PlaceholderText="Working directory" Text="{Binding WorkingDirectory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBox PlaceholderText="Use profile" Text="{Binding UseProfile, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                </muxc:SettingsCard>

                <muxc:SettingsCard Header="Test">
                    <StackPanel Spacing="8">
                        <TextBox PlaceholderText="https://example.com" Text="{x:Bind TestUrl, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        <Button Content="Test rule" Command="{x:Bind TestRuleCommand}" />
                        <TextBlock Text="{x:Bind TestError}" Foreground="{ThemeResource ErrorTextBrush}" TextWrapping="Wrap" />
                        <TextBlock Text="{x:Bind TestResult}" TextWrapping="Wrap" FontFamily="Consolas" />
                    </StackPanel>
                </muxc:SettingsCard>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
