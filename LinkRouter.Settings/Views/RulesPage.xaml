<Page
    x:Class="LinkRouter.Settings.Views.RulesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:controls="using:LinkRouter.Settings.Controls"
    xmlns:viewModels="using:LinkRouter.Settings.ViewModels"
    mc:Ignorable="d">
    <Page.DataContext>
        <viewModels:RulesViewModel />
    </Page.DataContext>

    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <muxc:CommandBar Grid.ColumnSpan="2">
            <AppBarButton Icon="Add" Label="Add" Command="{x:Bind ViewModel.AddRuleCommand}" />
            <AppBarButton Icon="Delete" Label="Delete" Command="{x:Bind ViewModel.DeleteRuleCommand}" CommandParameter="{x:Bind ViewModel.SelectedRule}" />
            <AppBarButton Icon="Copy" Label="Duplicate" Command="{x:Bind ViewModel.DuplicateRuleCommand}" CommandParameter="{x:Bind ViewModel.SelectedRule}" />
            <AppBarButton Label="Move up" Command="{x:Bind ViewModel.MoveUpCommand}" CommandParameter="{x:Bind ViewModel.SelectedRule}">
                <AppBarButton.Icon>
                    <FontIcon Glyph="&#xE74A;" />
                </AppBarButton.Icon>
            </AppBarButton>
            <AppBarButton Label="Move down" Command="{x:Bind ViewModel.MoveDownCommand}" CommandParameter="{x:Bind ViewModel.SelectedRule}">
                <AppBarButton.Icon>
                    <FontIcon Glyph="&#xE74B;" />
                </AppBarButton.Icon>
            </AppBarButton>
        </muxc:CommandBar>

        <ListView Grid.Row="1"
                  ItemsSource="{x:Bind ViewModel.Rules}"
                  SelectedItem="{x:Bind ViewModel.SelectedRule, Mode=TwoWay}"
                  SelectionMode="Single">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="viewModels:RuleEditorViewModel">
                    <StackPanel Spacing="4">
                        <TextBlock Text="{x:Bind DisplayName}" />
                        <TextBlock Text="{x:Bind Browser}" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <ScrollViewer Grid.Column="1" Grid.Row="1" Margin="16,0,0,0">
            <StackPanel Spacing="12">
                <controls:SettingsCard Header="Rule details">
                    <StackPanel Spacing="8" DataContext="{x:Bind ViewModel.SelectedRule, Mode=OneWay}">
                        <CheckBox Content="Enabled" IsChecked="{Binding Enabled, Mode=TwoWay}" />
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <ComboBox Width="160" ItemsSource="{x:Bind ViewModel.MatchTypes}" SelectedItem="{Binding Match, Mode=TwoWay}" />
                            <TextBox Width="320" PlaceholderText="Pattern" Text="{Binding Pattern, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                        <TextBox PlaceholderText="Browser path" Text="{Binding Browser, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBox PlaceholderText="Args template" Text="{Binding ArgsTemplate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBox PlaceholderText="Profile hint" Text="{Binding Profile, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBox PlaceholderText="User data dir" Text="{Binding UserDataDir, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBox PlaceholderText="Working directory" Text="{Binding WorkingDirectory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBox PlaceholderText="Use profile" Text="{Binding UseProfile, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                </controls:SettingsCard>

                <controls:SettingsCard Header="Test">
                    <StackPanel Spacing="8">
                        <TextBox PlaceholderText="https://example.com" Text="{x:Bind ViewModel.TestUrl, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        <Button Content="Test rule" Command="{x:Bind ViewModel.TestRuleCommand}" />
                        <TextBlock Text="{x:Bind ViewModel.TestError}" Foreground="{ThemeResource SystemFillColorCriticalBrush}" TextWrapping="Wrap" />
                        <TextBlock Text="{x:Bind ViewModel.TestResult}" TextWrapping="Wrap" FontFamily="Consolas" />
                    </StackPanel>
                </controls:SettingsCard>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
