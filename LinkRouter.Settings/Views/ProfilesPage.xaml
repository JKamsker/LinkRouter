<Page
    x:Class="LinkRouter.Settings.Views.ProfilesPage"
    x:Name="PageRoot"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:controls="using:LinkRouter.Settings.Controls"
    xmlns:viewModels="using:LinkRouter.Settings.ViewModels"
    xmlns:services="using:LinkRouter.Settings.Services"
    mc:Ignorable="d">
    <Page.DataContext>
        <viewModels:ProfilesViewModel />
    </Page.DataContext>

    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <muxc:CommandBar Grid.ColumnSpan="2">
            <AppBarButton Icon="Add" Label="Add" Command="{x:Bind ViewModel.AddProfileCommand}" />
            <AppBarButton Icon="Delete" Label="Delete" Command="{x:Bind ViewModel.RemoveProfileCommand}" CommandParameter="{x:Bind ViewModel.SelectedProfile}" />
            <AppBarButton Icon="Play" Label="Test launch" Command="{x:Bind ViewModel.TestLaunchCommand}" CommandParameter="{x:Bind ViewModel.SelectedProfile}" />
        </muxc:CommandBar>

        <ListView Grid.Row="1" ItemsSource="{x:Bind ViewModel.Profiles}" SelectedItem="{x:Bind ViewModel.SelectedProfile, Mode=TwoWay}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="viewModels:ProfileEditorViewModel">
                    <StackPanel>
                        <TextBlock Text="{x:Bind Name}" />
                        <TextBlock Text="{x:Bind Browser}" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <ScrollViewer Grid.Column="1" Grid.Row="1" Margin="16,0,0,0">
            <StackPanel Spacing="12">
                <controls:SettingsCard Header="Profile details">
                    <StackPanel Spacing="8" DataContext="{x:Bind ViewModel.SelectedProfile, Mode=OneWay}">
                        <TextBox PlaceholderText="Name" Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBox PlaceholderText="Browser path" Text="{Binding Browser, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBox PlaceholderText="Args template" Text="{Binding ArgsTemplate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBox PlaceholderText="Profile parameter" Text="{Binding Profile, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBox PlaceholderText="User data dir" Text="{Binding UserDataDir, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBox PlaceholderText="Working directory" Text="{Binding WorkingDirectory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                </controls:SettingsCard>

                <controls:SettingsCard Header="Detected browsers">
                    <StackPanel Spacing="8">
                        <TextBlock Text="{x:Bind ViewModel.DetectionError}" Foreground="{ThemeResource SystemFillColorCriticalBrush}" Visibility="{x:Bind ViewModel.DetectionErrorVisibility, Mode=OneWay}" />
                        <ItemsControl ItemsSource="{Binding Browsers}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <Button Content="Use" Click="UseDetectedBrowser_Click" />
                                        <TextBlock Text="{Binding Name}" />
                                        <TextBlock Text="{Binding Path}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </controls:SettingsCard>

                <controls:SettingsCard Header="Chromium profiles">
                    <ItemsControl ItemsSource="{Binding ChromiumProfileDirectories}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <Button Content="Use" Click="UseChromiumProfile_Click" />
                                    <TextBlock Text="{Binding}" />
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </controls:SettingsCard>

                <controls:SettingsCard Header="Firefox profiles">
                    <ItemsControl ItemsSource="{Binding FirefoxProfiles}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <Button Content="Use" Click="UseFirefoxProfile_Click" />
                                    <TextBlock Text="{Binding Name}" />
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </controls:SettingsCard>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
