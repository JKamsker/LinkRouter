<Page
    x:Class="LinkRouter.Settings.Views.ProfilesPage"
    x:Name="PageRoot"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:viewModels="using:LinkRouter.Settings.ViewModels"
    xmlns:services="using:LinkRouter.Settings.Services"
    x:DataType="viewModels:ProfilesViewModel"
    mc:Ignorable="d">
    <Page.DataContext>
        <viewModels:ProfilesViewModel />
    </Page.DataContext>

    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <muxc:CommandBar Grid.ColumnSpan="2">
            <AppBarButton Icon="Add" Label="Add" Command="{x:Bind AddProfileCommand}" />
            <AppBarButton Icon="Delete" Label="Delete" Command="{x:Bind RemoveProfileCommand}" CommandParameter="{x:Bind SelectedProfile}" />
            <AppBarButton Icon="Play" Label="Test launch" Command="{x:Bind TestLaunchCommand}" CommandParameter="{x:Bind SelectedProfile}" />
        </muxc:CommandBar>

        <ListView Grid.Row="1" ItemsSource="{x:Bind Profiles}" SelectedItem="{x:Bind SelectedProfile, Mode=TwoWay}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="viewModels:ProfileEditorViewModel">
                    <StackPanel>
                        <TextBlock Text="{x:Bind Name}" />
                        <TextBlock Text="{x:Bind Browser}" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <ScrollViewer Grid.Column="1" Grid.Row="1" Margin="16,0,0,0">
            <StackPanel Spacing="12">
                <muxc:SettingsCard Header="Profile details">
                    <StackPanel Spacing="8" DataContext="{x:Bind SelectedProfile, Mode=OneWay}">
                        <TextBox PlaceholderText="Name" Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBox PlaceholderText="Browser path" Text="{Binding Browser, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBox PlaceholderText="Args template" Text="{Binding ArgsTemplate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBox PlaceholderText="Profile parameter" Text="{Binding Profile, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBox PlaceholderText="User data dir" Text="{Binding UserDataDir, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBox PlaceholderText="Working directory" Text="{Binding WorkingDirectory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                </muxc:SettingsCard>

                <muxc:SettingsCard Header="Detected browsers">
                    <StackPanel Spacing="8">
                        <TextBlock Text="{x:Bind DetectionError}" Foreground="{ThemeResource ErrorTextBrush}" Visibility="{x:Bind DetectionErrorVisibility, Mode=OneWay}" />
                        <ItemsControl ItemsSource="{x:Bind Browsers}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate x:DataType="services:BrowserInfo">
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <Button Content="Use" Command="{x:Bind ((viewModels:ProfilesViewModel)PageRoot.DataContext).UseDetectedBrowserCommand}" CommandParameter="{x:Bind}" />
                                        <TextBlock Text="{x:Bind Name}" />
                                        <TextBlock Text="{x:Bind Path}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </muxc:SettingsCard>

                <muxc:SettingsCard Header="Chromium profiles">
                    <ItemsControl ItemsSource="{x:Bind ChromiumProfileDirectories}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate x:DataType="x:String">
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <Button Content="Use" Command="{x:Bind ((viewModels:ProfilesViewModel)PageRoot.DataContext).UseChromiumProfileCommand}" CommandParameter="{x:Bind}" />
                                    <TextBlock Text="{x:Bind}" />
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </muxc:SettingsCard>

                <muxc:SettingsCard Header="Firefox profiles">
                    <ItemsControl ItemsSource="{x:Bind FirefoxProfiles}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate x:DataType="services:FirefoxProfileInfo">
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <Button Content="Use" Command="{x:Bind ((viewModels:ProfilesViewModel)PageRoot.DataContext).UseFirefoxProfileCommand}" CommandParameter="{x:Bind}" />
                                    <TextBlock Text="{x:Bind Name}" />
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </muxc:SettingsCard>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
